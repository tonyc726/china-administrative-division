> 生成:2026-07-03 ｜ 方法:7 agent 并行调研/方案设计(Workflow)→ synthesize。承接 P0-P3 完成后的剩余优化盘点。
> 定位:后续执行依据。代码方案项标注「🔧 可 worktree 隔离并行实现」(对齐已完成的 P0);调研项给 go/no-go。
> 上游:[研究报告](./数据采集现状评估与提升路径.md) · [P0-P3 计划](./采集能力提升实施计划.md) · [运维手册](./采集运维手册.md)

# 采集基础设施 · 优化清单 v2

> 输入:7 项并行调研/方案结论。分组口径 = 验收确定性 × ROI。代码方案已标注可 worktree 隔离并行实现(对齐已完成的 P0 模式);调研项给明确 go/no-go。

---

## 一、立即可做(确定性收尾 · 低成本高确定 ROI)

> 说明:三项数据源调研均为「否定放弃」,但其调研过程沉淀出**无需再试验、可直接落地的低成本加固动作**。它们不新建采集通道,只做固化/存证/对齐,风险可控,建议直接排期。

### 1.1 level5 村级「冻结地板」正式固化
- **一句话**:五渠道(民政厅/公安地址库/腾讯 API/村务公开/三调)全部证否,无一满足「带 GB2260 码 + 程序化 + 活更新」,正式确立 NBS 2023-06-30 为冻结地板,采集侧永久排除村级轮询。
- **工作量**:低(仅文档 + 已固化测试确认)。
- **验收**:① `diffToPatch` 默认 `levels=[1,2,3,4]`(`test/diff.test.ts` 已固化);② 村级增量仅经 Patch 通道(旁支③ NLP 提取器 draft + 置信度分级);③ 冻结结论与证据落 `docs/spike-village-level5.md`。
- **关键风险**:未来若出现省级「智慧民政」/政务开放平台放出带码村级名录(低概率),需重估;村务公开门户可作旁支③ NLP 原料纳入抓取清单(不冲突)。
- **涉及文件**:`docs/spike-village-level5.md`、`packages/crawler`(diff 默认层级)。

### 1.2 邮编/区号:保留 ip138 + diff 级加固(div_code 对齐 + 快照冻结 + 校验层)
- **一句话**:官方源(中国邮政/工信部)均无开放数据、且**不存在 GB/T 邮编标准**,替换路径证否;保留 ip138 为主源,叠加三层加固而非真替换。
- **工作量**:低。
- **验收**:① 文档记录「官方无开放源 + 无 GB/T 邮编标准」证否结论(附引用);② `@cndiv/source-postal` 输出新增 `div_code` 字段,可与主线 GB2260/NBS JOIN,重名/冲突单列 `conflicts` 且可统计;③ 校验层对已知区号规则(直辖市 010/021/022/023、8 大区中心 020/024/025/027/028)自动断言,对第二源 diff 输出漂移条数(只告警不覆盖);④ 采集运维手册标注邮编/区号为「近静态·年度/按需」节律。
- **关键风险**:ip138 仍是第三方(已改版过一次),靠快照 + 校验层兜底;name→GB2260 对「市辖区/县」占位层与跨省重名需人工规则,存在残留冲突;若需求下探到「镇/投递局」级,所有可得源均只到县级(需另立需求)。
- **涉及文件**:`packages/crawler/src/ip138.ts`、`packages/crawler/src/postal.ts`、`docs/采集运维手册.md`。

### 1.3 商业地图源条款闭环存证
- **一句话**:高德/天地图/腾讯/百度四大商业源全部核实,均为「三重禁止存储再分发」(百度 3.3.4 明文禁止「生成或用于数据库」最严),dmfw 仍是**唯一合法可再分发路径**,维持现有采集架构不变。
- **工作量**:低(一次人工复核 + 截图存证)。
- **验收**:腾讯 `terms.html` 6.2(3) 再分发禁止条 + 4.5 数据归属条、百度 `open/law` 3.3.4 数据库禁止条,截图归档;结论「仅 dmfw 可用于再分发」闭环成立。
- **关键风险**:条款页动态渲染,编号可能随版本漂移 → 落库前人工复核官方页面截图存证;商业授权渠道理论存在但几乎不可能授予 MIT 公开再分发权,未深入洽谈(判定无意义)。
- **涉及文件**:`docs/采集运维手册.md`(存证附录)。

---

## 二、需先试验(方案已就绪 · 待受控验证后转「可做」)

> 三项均为**代码方案**,设计完备但缺一次端到端实测/标定,故 verdict = 需进一步试验。**均可 worktree 隔离并行实现**(参照 P0);但彼此存在数据/管线依赖,见第四节执行顺序。

### 2.1 dmfw API 抖动稳定化(多抓 UNION + 基线异常收缩检测 + 写后即稳缓存) ✅ 已实现(commit `763cf4a`)

> **执行结果**：按方案落地——`unionChildren` 纯函数(幂等/交换/递归并孙层)、`crawl-all` critical 浅层多抓收敛 + 内部节点基线收缩重抓 + 缓存写 UNION、`run.ts --stabilize=on` 集成(基线仅取 level≤maxLevel 排除 level5 误判)。全 opt-in 向后兼容(不传单抓)。**F7 幽灵不复活硬约束**:基线只触发重抓不注入,单测已固化。8 单测(UNION/critical/基线收缩/F7/兼容/root sanity)全绿;端到端宁夏 270 条与 T1.1 一致。前置 `cndiv hydrate` 已完成(665271 records,cache.db 就绪)。
- **一句话**:got 的 retry 对「HTTP 200 但 children 被截断」完全无感,且校验前就写缓存会复现毒数据;方案在**浅层(全国根)无条件多抓 UNION、内部节点仅在相对基线收缩时才重抓**,稳态额外请求 <+5%,毒缓存根治。
- **工作量**:中。ROI:**高**。
- **验收**:① `unionChildren` 单测(幂等/交换律/孙层递归并集);② 注入 mock 丢子,`fetchStable` 的 UNION 覆盖全集且 `jitter.counts` 记录抖动;③ 构造「基线有但 live 连抓 3 次都无」的撤销场景,输出**不含**该节点(F7 幽灵节点不复活);④ `shrunkVsBaseline` 直接子 + 孙层双查生效;⑤ 全量(稳定数据 + 基线)额外请求 <+5%,缓存全命中重跑请求≈0(断点续爬不回归);⑥ 根返回 <31 省触发告警回调。
- **关键风险**:① dmfw 抖动真实概率分布未知(transient vs 服务端 systematic 截断)——**需一次受控复现(同 root 连查 N 次统计 count 方差)标定 `criticalAttempts/anomalyAttempts`**,这是「需试验」的核心;② 若为 systematic 截断,多抓不收敛,须改走 xzqh 事件流交叉补齐(超范围);③ 首跑无基线时市/县层 partial 截断仅靠 walk 自愈有残留——建议首跑用 `criticalMaxLevel=2` 全量稳定化跑一版作「黄金基线」,此后增量回落 `criticalMaxLevel=0`;④ baseline 来源需接 `@cndiv/reader` 读上次 cache.db 或扫描 FsCache 目录(**软依赖 reader**)。
- **涉及文件**:`packages/crawler/src/dmfw.ts`、`crawl-all.ts`、`cache.ts`。

### 2.2 xzqh resolver 生产化(@cndiv/reader 注水 cache.db 名称索引替换 baseline CSV) ✅ 已实现(commit `3deb134`)

> **执行结果**：新增 `xzqh-resolver.ts::buildCacheResolver`（`getByLevel(1..5,year)` 预载建 `name→Set<code>` 索引，排除 `PLACEHOLDER_NAMES`+deprecated，同名歧义记入 `ambiguous` 并返回 null 落 unresolved）；`run-xzqh.ts` 装 `assembleResolver` 三态优先级 **cache.db→CSV→占位**（失败软降级不中断），新增 `--cache`/`--snapshot-year`，`finally close()` 释放 sqlite 句柄，validate 失败改 `exitCode`（保 finally）。5 单测覆盖验收①②④⑤⑥。**端到端**：2023 快照 407187 名索引、2024 五条变更全解析、两「设立」新县正确落 unresolved。风险①（cache.db 未生成）已随 `cndiv hydrate` 完成消解。签名未改（Occam，风险②未触发）。

- **一句话**:reader API 完备、机制已验证,用 `getByLevel(1..5, year)` 一次性预载建内存 `name→code` 索引替换 baseline CSV;**同名歧义一律落 unresolved + 记全候选,绝不臆造码**。
- **工作量**:中。ROI:中。
- **验收**:① 备含 2023 快照的 cache.db,`run-xzqh --year=2024 --snapshot-year=2023`:rename/abolish/move 命中率 ≥ CSV 版;② 含「朝阳区」的变更文本落 unresolved 且 `ambiguous` 输出含 `[110105,211321]` 两候选,零错误 op;③ cache.db 缺失 → 打印警告退占位/CSV,exit 0 不崩;④ 索引 Map 不含任一 `PLACEHOLDER_NAMES` 键;⑤ 进程结束无 better-sqlite3 句柄泄漏(`close` 被调用);⑥ `validatePatch` 守门,unresolved 与 ambiguous 分别计数。
- **关键风险**:① **`~/.cndiv/cache.db` 当前未生成,方案未能端到端实跑(须先 `cndiv hydrate`)**——这是「需试验」根因,亦是硬前置依赖;② 现签名 `CodeResolver=(name)=>string|null` 无 parent 上下文,上下文歧义无法在签名内消解,若实测 unresolved 比例过高,二阶方案须扩展 `ResolveContext` 传 parent 提示(ripple extractor API,单列另评);③ `snapshotYear` 取 `max(years)` 假设最新快照即变更前基线,生产建议显式传 `--snapshot-year`;④ 异体字/别名(峰/峯)不归一,落 unresolved。
- **涉及文件**:`packages/crawler/src/run-xzqh.ts`、`packages/reader/src/index.ts`、`packages/extractor/src/{types,resolve}.ts`、`packages/crawler/{src/baseline.ts,package.json}`。

### 2.3 xzqh↔dmfw 产物合并去重(mergePatches 纯函数 + merge-patches CLI + 置信度落地) ✅ 已实现(commit `c7144f2`)

> **执行结果**：`data-protocol/merge.ts::mergePatches` 纯函数（冲突键=code、优先级 `xzqh>community>dmfw` 可 `--priority` 配、败者整组落 `conflicts` sidecar、add-remove 保留 add、完全相同 op 去重、恒序 add→move→update→remove、幂等、`apply_after` 错配抛错、置信度取最高）+ `validateMerged` 兜底；`cli/merge-patches.ts` + `cndiv merge-patches --dir --out [--priority]`（管线按文件名/author 启发式判定，成品 + `<out>.conflicts.json` sidecar）。9 单测覆盖验收①，端到端 dmfw+xzqh 同 code→xzqh 胜/dmfw 落 sidecar/正交 op 保留。**偏差**：字段级合并留 v2（风险①，正交 op 连带丢弃由 sidecar 显式列出）。~~`source_pipeline` 未入 schema~~ → **风险③已后续关闭（commit `ace2877`）**：`source_pipeline` 已升为 PatchMeta 一等 optional 字段，生产者自盖（dmfw/xzqh），merge 优先读戳、缺省才回退启发式（兼容老 patch，验收⑤仍成立）。

- **一句话**:两管线都落 `patches/<year>/*.json` 但 apply-patch 一次只吃一个、无跨文件去重/冲突检测;方案新增**纯函数 `mergePatches` + CLI 子命令**,原料落 `raw/`、成品 `<year>.merged.json`,冲突键 = code、优先级 `xzqh>community>dmfw`,apply-patch 单文件事务不变。
- **工作量**:中。ROI:**高**。
- **验收**:① `merge.test.ts`:xzqh vs dmfw 同 code update → xzqh 胜、dmfw 落 `conflicts.dropped`;add vs remove → 保留 add;完全重复去重 `deduped=1`;`apply_after` 不同抛错;输出 op 恒序 `add→move→update→remove` 再按 code;`mergePatches` 幂等;② 合并成品必过 `validatePatch`(CI `validate-patches.mjs` 绿);③ E2E:`run(raw)+run-xzqh(raw)→merge-patches→apply-patch` 后 cache.db 同码取 xzqh 值、`patch_history` 只 +1 行(未双 apply);④ 冲突 sidecar 报告列出每条被舍弃 op(无静默丢弃);⑤ 旧手写 patch(无 `source_pipeline`)经 author/文件名回退正确归类不崩。
- **关键风险**:① **冲突键取 code → 败者该 code 上所有 op 整组丢弃**(如 xzqh 只改名、dmfw 同 code 带正交 move,move 被连带丢)——报告显式列出,字段级合并留 v2(Occam);② community 优先级(dmfw 之上、xzqh 之下)是判断值,运维手册 §4 未明确,需与维护者确认或做 `--priority` 可配;③ `source_pipeline` 加字段**需同步改两个 producer** 才生效,未改前靠 author/文件名启发式有误判风险;④ update/remove/move 无 per-op 溯源是 schema 固有限制,溯源退到 sidecar(DB 内按 op 存来源需另立 schema 演进项)。
- **涉及文件**:`packages/data-protocol/src/{schemas/patch.ts,merge.ts(新)}`、`packages/crawler/src/{run.ts,run-xzqh.ts,diff.ts}`、`packages/cli/src/{merge-patches.ts(新),apply-patch.ts,cli.ts}`、`scripts/validate-patches.mjs`、`docs/采集运维手册.md`。

### 2.4 省级民政厅半年度乡级代码 · 权威校验/增量监控点(调研 → PoC 试验) ✅ PoC 达成(commit `3b66f6c`)

> **执行结果**：`crawler/prov-township.ts`——`PROVINCE_TOWNSHIP_REGISTRY`（川✅确认/鲁浙🟡候选/粤⛔反例，含真实栏目+最新期 URL）、`parseTownshipHtmlTable`（format-tolerant 抽 9 位码+名称）、`townshipRowToDivision`（9→12 尾补 000、parent=前6位+000000、level4、`mca_decree` 置信 95）；`run-prov-township.ts`（`--file` 本地 HTML→解析→归一→对 cache.db 基线 diff(levels=[4])→抑制 remove→validatePatch；`--registry` 打印注册表；bin `cndiv-prov-township`）。6 单测。**端到端**：四川 fixture 对 cache.db 3111 基线产 update(更名)+add(新增)、过 validatePatch。**验收①法条已核 ②川确认+鲁浙候选 ③PoC 闭环达成**。
>
> **偏差/边界（诚实记录）**：四川 shtml 直连反爬（curl/WebFetch 均 403/404，仅搜索引擎可索引），故**未活体抓取**，PoC 只验证「拿到字节后」的解析→diff→校验闭环；活体抓取（浏览器形态/OSS xlsx 直链）须逐省适配，另立工程项。**验收④「2026-08 复查川鲁浙是否发『截至 2026-06-30』版」为未来动作**，到期执行。

- **一句话**:79 号令第十六条第二款(gov.cn 全文)明确「省级民政部门每年 1 月和 7 月发布截至上月末本地区乡级行政区划代码」——**独立于 dmfw 年更、粒度到乡级 9 位码、半年频次**;四川/山东已履约,广东为反例,建议先 3-5 省 PoC 验证节律真实性与解析可行。
- **工作量**:高(异构无统一 API,须逐省适配)。ROI:中。
- **go/no-go**:**有条件 GO(先 PoC,后决定是否 34 省全铺)**。理由:法条确立 + 川鲁已落地,增量价值在「频次 + 乡级权威印证 + 批复日期证据」而非新增层级,值得建监控点但不宜一次全铺。
- **验收**:① 79 号令条款已核(完成);② ≥3 省官网可定位 2026-01 截至 2025-12-31 乡级代码表且含 9 位码(四川已确认,山东/浙江补证形态);③ PoC:抓四川 `quhuaxinxi` 栏目最新期 → 解析乡级 9 位码 → 与 source-2023 基线 diff → `operations` 过 `validatePatch`;④ **2026-08 复查川/鲁/浙是否已发「截至 2026-06-30」版**,判定半年节律稳定性。
- **关键风险**:① 各省栏目 URL/形态异构(网页表/xlsx/PDF/公众号)、URL 会漂移无 SLA;② 覆盖不均(广东仍纸质滞后),首年履约率待观测;③ 增量边际收益有限(dmfw 年更已覆盖 level4);④ 口径须与 dmfw/NBS 层级对齐(参见 dmfw-nbs 层级差异记忆),避免伪变更;⑤ **首个 7 月周期(截至 2026-06)尚未观测到实际发布**,半年节律未经完整验证。
- **涉及/引用**:gov.cn 79 号令全文、`mzt.sc.gov.cn/scmzt/quhuaxinxi/`(四川专栏)、新建「省级民政厅乡级代码半年监控注册表」(省码→栏目 URL + 形态类型)。

---

## 三、放弃/否定(证否闭环 · 不再投入采集通道)

| 项 | 结论 | 核心否定理由 | 引用锚点 |
|---|---|---|---|
| **村级 level5 五渠道深挖** | ❌ no-go(作全量源) | 民政厅=下游派生无独立增量;公安地址库=内网 + 合规红线;腾讯/百度 API 止于街道级且无 GB2260 码;村务公开=治理机制非带码名录;三调=坐标图斑体系非区划码且时点冻结。dmfw 地名代码 ≠ GB2260 区划码。 | `docs/spike-village-level5.md`、NBS 2023 编制规则、腾讯 webServiceDistrict |
| **邮编/区号权威源替代 ip138** | ❌ no-go(替换源) | 中国邮政仅网页表单无 API、比 ip138 更脆;工信部区号是 YD/T 规则文档非数据集;**GB/T 邮编标准不存在**(GB/T 2260 只含区划码)。数据近静态、更新诉求极低。 | `openstd.samr.gov.cn` GB/T 2260、`dey.11185.cn`、国家邮政局辟谣 |
| **腾讯/百度地图许可与层级** | ❌ no-go(再分发源) | 腾讯 6.2(3) 禁 API 转供 + 4.5 数据归属;百度 3.3.4 明文禁「生成或用于数据库」(最严)。四大商业源无一放开存储再分发,腾讯乡镇级深度虽超高德但因禁落库对 MIT 项目无价值。 | `lbs.qq.com/terms.html`、`lbsyun.baidu.com/index.php?title=open/law` |

> 三项否定均已闭环,其**低成本收尾动作**见第一节 1.1/1.2/1.3。共同前提风险:各官方条款/接口页面动态渲染,证据须截图存证以防版本漂移。

---

## 四、推荐执行顺序(依赖拓扑 + 并行度)

### 依赖关系图
```
[前置] cndiv hydrate → 生成 ~/.cndiv/cache.db  ← 硬阻塞 2.2,软支撑 2.1(baseline)
        │
        ├──> 2.2 resolver 生产化 ──┐
        │    (依赖 hydrated db)     │
        │                          ├──> 2.3 merge 去重
2.1 dmfw 稳定化 ────────────────────┘    (依赖两条管线 raw patches 就位:
   (独立·软依赖 reader 取 baseline)        dmfw run + xzqh run 均需先稳定产出)

[完全独立·任意并行]
  第一节 1.1 / 1.2 / 1.3(否定项收尾)
  2.4 省级民政厅乡级 PoC(独立调研,含 2026-08 复查时点)
```

### 建议排期

**波次 0 · 前置基建(串行阻塞点,优先做)**
- `cndiv hydrate` 生成 cache.db。**这是 2.2 的硬前置、2.1 baseline 的软支撑**,不做则 2.2 无法端到端验收、2.1 首跑无黄金基线。

**波次 1 · 并行(3 条 worktree + 收尾动作齐发)**
- 🔧 **2.1 dmfw 稳定化**(高 ROI、与其余代码项无耦合,可独立 worktree)——先做一次「同 root 连查 N 次」受控复现标定参数,再实现。
- **1.1 / 1.2 / 1.3** 三项低成本收尾(纯文档/加固,与代码项零冲突,任意并行)。
- **2.4 省级乡级 PoC**(独立调研,四川已具解析条件;注意 2026-08 需回访复查节律)。

**波次 2 · 串行(依赖波次 0 产出的 cache.db)**
- 🔧 **2.2 resolver 生产化**——依赖 hydrated cache.db 实测同名歧义率/命中率。

**波次 3 · 串行(依赖两条管线就位)**
- 🔧 **2.3 merge 去重**——须 dmfw 管线(2.1 稳定后产出)与 xzqh 管线(2.2 resolver 生产化后产出)**均能稳定生成 raw patches** 才能端到端 E2E 验收(否则冲突用例缺真实原料)。

### 并行/串行判定小结
- **完全独立可并行**:2.1、2.4、1.1、1.2、1.3(互不触碰同一文件/数据)。
- **串行链**:`hydrate(波次0) → 2.2 → 2.3`;其中 2.3 额外等待 2.1 稳定(dmfw 侧原料质量)。
- **软依赖**:2.1 的 baseline 构建可复用 `@cndiv/reader`/FsCache,若波次 0 未完成可先用扫描 FsCache 目录降级,不阻塞 2.1 主体开发。
