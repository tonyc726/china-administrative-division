# 注册表指向 npm 官方（@cndiv org 所在）——项目自包含、与 CI 一致，
# login / whoami / publish 均走官方源，覆盖全局 ~/.npmrc 的淘宝镜像。
# 如需加速 JS 依赖安装，可临时：pnpm install --registry=https://registry.npmmirror.com
registry=https://registry.npmjs.org/

# 说明：原有 disturl / *_cdnurl / sqlite3_binary_host_mirror 等二进制镜像键已移除——
# 它们是旧脚手架遗留（对应 chromedriver/electron/puppeteer/node-sass 等本项目无的依赖），
# 且被 npm 判为 "Unknown project config、下个大版本将停止工作"，每条命令刷 12 行告警。
# 唯一原生依赖 better-sqlite3 用 prebuild-install 从 GitHub 拉预编译包，不读这些键。
